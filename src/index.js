import React from "react";
import ReactDOM from "react-dom";
import App from './App';
//funcion que recive 2 parametros, 1ro jsx que queremos cargar, 2do dónde lo queremos cargar
ReactDOM.render(
    <App/>,
    document.querySelector('#root')
);
